#version 410 core

// in vec3 FragPos;
// in vec3 Normal;

// uniform vec3 lightDir;
// uniform vec3 lightColor;
// uniform vec3 objectColor;

out vec4 FragColor;

void main()
{
    FragColor = vec4(1.0, 0.5, 0.2, 1.0);
}